using UnityEngine;

namespace Quests {

    /// <summary>To be used as base type of context objects that define the state of an ongoing quest.</summary>
    public abstract class QuestContext : MonoBehaviour { }
}
